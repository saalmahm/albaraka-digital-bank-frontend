import { NgModule } from '@angular/core';

/**
 * CoreModule
 * - Contient les services globaux, interceptors, guards, etc.
 * - Doit être importé une seule fois dans l'application (via les providers, plus tard).
 */
@NgModule({
  providers: [
    // À compléter plus tard 
  ]
})
export class CoreModule {}